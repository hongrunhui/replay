diff --git a/BUILD.gn b/BUILD.gn
index 1234567..abcdefg 100644
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -1000,6 +1000,8 @@ v8_source_set("v8_base_without_compiler") {
     "src/profiler/weak-code-registry.h",
     "src/profiler/weak-code-registry.cc",
+    "src/recorder/recorder.h",
+    "src/recorder/recorder.cc",
     "src/regexp/experimental/experimental.cc",
     "src/regexp/experimental/experimental.h",
   ]
diff --git a/src/init/v8.cc b/src/init/v8.cc
index 2345678..bcdefgh 100644
--- a/src/init/v8.cc
+++ b/src/init/v8.cc
@@ -50,6 +50,7 @@
 #include "src/profiler/heap-profiler.h"
 #include "src/profiler/tracing-cpu-profiler.h"
+#include "src/recorder/recorder.h"
 #include "src/snapshot/embedded/embedded-file-writer.h"
 #include "src/snapshot/read-only-deserializer.h"
 #include "src/snapshot/snapshot.h"
@@ -150,6 +151,10 @@ void V8::InitializePlatform(v8::Platform* platform) {

 void V8::Dispose() {
   CHECK(platform_);
+
+  // 停止录制（如果正在录制）
+  if (internal::Recorder::GetInstance()->IsRecording()) {
+    internal::Recorder::GetInstance()->StopRecording();
+  }
+
   if (FLAG_predictable) {
     // In predictable mode, we need to ensure that all tasks are run before
